[package]
name = "airborne"
version = "0.1.0"
edition = "2024"
description = "A heavily-typed, generic Rust statistics library with compile-time Sample/Population semantics."
keywords    = ["statistics", "math", "generics", "sample", "population"]
license     = "MIT"

[dependencies]
num-traits = "0.2"
thiserror  = "2.0"

[dev-dependencies]
approx = "0.5"
